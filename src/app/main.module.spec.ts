import 'reflect-metadata';

import { moduleOptionKeys } from '@/core/utils/validate-module-options.util';

import { MainModule } from './main.v2.module';

describe('MainModule', () => {
  it('should be defined', () => {
    expect(MainModule).toBeDefined();
  });
});
