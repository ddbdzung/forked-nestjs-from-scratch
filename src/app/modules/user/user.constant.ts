// Compulsory constants for user module
export const USER_MODEL = 'user';
export const PREFIX_USER_MODULE = 'users';

// Shared constants for module scope (inside user module)

// Shared constants for service scope (global scope)
